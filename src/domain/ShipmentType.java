// 'package' declaration: this enum type is declared in the 'domain' package.
package domain;

/**
 * Enum for creation-time shipment type selection.
 */
public enum ShipmentType {
	
	// Enum constants: identifiers separated by commas; each constant is a singleton instance of ShipmentType.
	STANDARD,
	EXPRESS,
	FRAGILE
}
